{
  "name": "bq-json-struct",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "release-build": "tsc && vite build --base=/bq-json-struct-conv/ --outDir=docs/"
  },
  "devDependencies": {
    "typescript": "^4.6.4",
    "vite": "^3.2.3"
  },
  "dependencies": {
    "typist-json": "^1.0.1"
  }
}
